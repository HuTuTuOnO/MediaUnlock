# Go
/dist/
*.exe
*.test
*.out

# SQLite data
/server/data/*.db
/server/data/*.db-*

# Local config (keep *.example.yml)
/server/config.yml
/agent/config.yml

# Node / Vite
/web/node_modules/
/web/dist/
/web/.vite/

# 嵌入 Server 的前端构建产物(由 scripts/build.sh 同步进来)
# web/ 整目录忽略,只保留 .gitkeep 让目录存在 —— embed 要求目标非空
/server/internal/static/web/*
!/server/internal/static/web/.gitkeep

# Editor / OS
.DS_Store
.idea/
.vscode/

# 助手的工作笔记,留在本地就够了
.workbuddy-ai/
