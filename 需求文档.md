# MediaUnlock 流媒体解锁管理系统 — 需求文档

> 给 AI / 开发者的项目说明书。技术栈:Go 全家桶。
> Server / Agent / Web 三端;Agent 与 Server 通过 **REST + Token** 通信,Agent 按 **cron(`scheduler`)** 定时执行。
> 解锁检测能力复用 [MediaUnlockTest](https://github.com/HsukqiLee/MediaUnlockTest)(Go 编写,按地区/AI 分类的数百个检测项)。

---

## 1. 项目概述

一套**流媒体解锁检测与分发管理系统**,由三部分组成:

| 组件 | 语言/框架 | 职责 |
|------|-----------|------|
| **Server** | Go (Gin) + GORM + **SQLite** | 提供 API,统一存储节点、平台、解锁数据、用户与配置 |
| **Agent** | Go(单文件二进制,交叉编译分发;**fork MediaUnlockTest 的 `pkg/` 检测代码内置**) | 部署在各节点上,分 **client / server** 两种模式运行 |
| **Web** | Vite + React + TS + **shadcn 风格原语**(Radix UI;布局参考 [satnaing/shadcn-admin](https://github.com/satnaing/shadcn-admin)) | 管理后台:登录、仪表盘、节点、平台、解锁历史、系统设置 |

核心价值:集中检测各解锁节点对各流媒体平台的解锁情况,并**为 soga 服务端软件自动生成分流配置**,让落地节点把**自己解不开、而解锁节点能解**的那些平台流量,精准转发到对应解锁节点(本机已能解的直接走本地)。面向 soga 运营方,非终端用户。

### 1.1 技术栈选型

| 端 | 技术 | 打包/形态 |
|---|---|---|
| **Server** | Go 1.26.4 + Gin + GORM + SQLite;配置用 `config.yml` | **单二进制** |
| **Agent** | Go 单二进制,内置 fork 的 `pkg/`;配置用 `config.yml` | **单二进制** |
| **Web** | Vite 7 + React 19 + TS 5.9 + Tailwind 4 + shadcn 风格原语(Radix UI)+ `react-router-dom` v7(HashRouter) | 静态资源,构建后嵌入 Server |

**Server / Agent 依赖库(选型已定):**

| 用途 | 库 |
|---|---|
| Web 框架 | `gin-gonic/gin` |
| ORM | `gorm.io/gorm` + `glebarez/sqlite`(纯 Go SQLite 驱动,**免 CGO**,便于交叉编译单二进制) |
| JWT | `golang-jwt/jwt/v5` |
| 密码哈希 | `golang.org/x/crypto/bcrypt` |
| cron(6 段含秒) | `robfig/cron/v3`(`WithSeconds()`) |
| YAML 解析 | `gopkg.in/yaml.v3` |
| 日志 | 标准库 `log/slog` |
| 代理服务(node 模式起) | GOST:`go-gost/x` + `go-gost/core`(按节点 `type` 启动 HTTP 或 SOCKS5,均支持账号密码认证;嵌入 Agent 单文件) |
| 解锁检测 | fork 自 MediaUnlockTest 的 `pkg/`(含 `tls-client` 等) |

> Server 与 Agent 均编译为**单二进制**,配置文件均为 `config.yml`(各自独立);Web 为静态资源,由 Server 托管或独立部署皆可。

> **解锁节点为裸机**(不预装代理软件),故 Agent `server` 模式**自行起本机 SOCKS5/HTTP 代理服务**(嵌入 GOST,按节点 `type` 选),供落地节点连接;不依赖节点上任何外部代理软件。

---

## 2. 名词约定

- **节点 (Node)**:一个代理线路端点(**只指解锁节点**)。含 `alias`(唯一标识)、`type`(`socks5`/`http`)、`host`/`port`、账号密码、`token`(Agent 认证)。连接信息直接存库,供落地节点使用。落地节点不入库。
- **平台 (Platform)**:一个流媒体/AI 服务(Netflix、Disney+、YouTube Premium、ChatGPT 等)。`name` 与 MediaUnlockTest 检测名一一对应;`rules` 是**分流域名/路由**(如 `domain:sky.de`,生成配置用,**非检测规则**)。
- **解锁节点 (Unlock / node-mode)**:真正"出国"的出口服务器,IP 决定它能解锁哪些平台(如日本原生 IP 能解 Netflix 日本)。运行 Agent 的 **node 模式**:拉取自身节点信息 → 起本机 SOCKS5/HTTP 服务 → 检测自己 → 带节点 `token` 上报 Server。连接信息在节点表维护,Agent 据此起服务,落地节点据此来连。
- **落地节点 (Landing / Client-mode)**:用户实际连接的入口服务器(离用户近、速度快,但 IP 可能解不开流媒体)。大部分流量直接从这里出,只有需要解锁的平台流量才转发到解锁节点。运行 Agent 的 **client 模式**:①本地跑解锁检测,得出**本机未解锁**的平台;②用全局只读 Token 读取 Server 的已解锁数据;③对"本机未解锁、但 API 中有解锁节点能解"的平台,生成分流配置转发到解锁节点(本机已解锁的直接走本地,不分流)。**本地检测仅用于对比、不上报;不心跳,不入节点表。**
- **解锁关联 (node_platforms)**:节点×平台的多对多关系,记录"某节点当前解锁某平台"。仅成功解锁(status==1)入表,是 client 下发的**唯一数据源**。
- **解锁历史 (unlocks)**:每次 **node 模式**检测上报的完整留痕(status/region/info/err),仅供后台查看,不参与下发。
  在节点/平台**存活期间**完整留痕;**删除节点或平台时会连带清理其解锁历史**(避免留下无法关联的孤儿记录)。

数据流向图:

```
终端用户 ─▶ 落地节点(跑 soga,client Agent 喂配置)─▶ 解锁节点(原生IP,能解Netflix)─▶ Netflix
```

> 本系统面向的是**运营 soga 的服务端**,负责给落地节点上的 soga 自动生成分流配置;终端用户不直接使用本系统。

---

## 3. Server(API 服务)

### 3.1 功能模块

1. **登录 / 认证**
   - 管理员账号密码登录,签发 JWT(供 Web 后台用)。
   - **初始管理员**:首次启动若无 admin,自动创建并**生成随机密码打印到启动日志**(不硬编码默认弱口令);首登后建议改密。
   - **node 模式 Agent**:用**节点专属 `token`**(在节点表里)访问 `node` / `report` 接口。
   - **client 模式 Agent**:只读 `unlocked` 接口;用一个**全局只读 Token**(系统设置 `token` 键)即可,不与具体节点绑定。
   - 权限:系统仅**单一管理员角色**,登录即全权(无 viewer / 只读角色)。

2. **节点列表 (Node)**
   - **节点表只存 node(解锁)节点**——每个解锁节点运行 node 模式 Agent,只测自己、只上报自己。client(落地)节点不入库。
   - 每个节点有独立 `token` 做 Agent 认证。
   - `alias`(节点标识)**全局唯一**,作为业务引用键(上报/聚合时用它引用节点)。
   - `type` 当前支持 `socks5` / `http`(**统一小写**,与 agent 的 GOST handler 对应);
     其他值 agent 起不了服务,后端会直接 400。`value3~6` 预留给未来其他协议(如 SS/VMess/VLESS 需要的额外字段)。
   - CRUD 节点;生成/重置 token;展示每个节点已解锁平台数、最近一次上报时间。
   - **`status` 禁用**:该节点的 server Agent **照常起服务、检测、上报**(数据照常入库);
     `GET /api/agent/unlocked` 是**全量下发**,禁用节点也会带 `status: 0` 发出去,由 client Agent 自行决定不把流量导向它。
   - (字段定义见 3.3 数据模型)

3. **平台列表 (Platform)**
   - 维护平台清单;CRUD。
   - **`rules` 是分流规则,不是检测规则**:供 client 生成配置时使用(如 `domain:sky.de`、`domain:netflix.com` 等域名/路由列表),用来把该平台的流量分流到能解锁它的解锁节点。检测是否解锁另由 MediaUnlockTest 完成,与 `rules` 无关。
   - `platforms.name` 与 MediaUnlockTest 的检测项名称一一对应(手动维护;后期需要再考虑一键同步)。
   - **`status` 禁用**:该平台**照常检测、照常写入 node_platforms 与 unlocks**(数据完整保留);
     `GET /api/agent/unlocked` **全量下发**并带 `status: 0`,由 client Agent 自行跳过。
   - (字段定义见 3.3 数据模型)

4. **解锁数据** —— 拆成两张表,职责分开:

   **(a) 节点-平台关联表 (node_platforms)** —— 当前最新状态,**下发数据源**
   - 节点与平台的**多对多**关联:一条记录表示"某节点当前解锁某平台"。
   - server Agent 每次上报后,Server 用最新结果**覆盖/更新**这张表(唯一键 `node_id + platform_id`)。
   - **只有 `status == 1`(成功解锁)的平台才写入/保留在关联表**;其他状态(受限/封禁/失败等)不进关联表(若之前有该关系则删除),但仍完整记入历史表。
   - client 拉取 `unlocked` 时,直接查这张表 → 得到"哪些节点解锁哪些平台" + 平台 rules + 节点连接信息。
   - 只记录关系本身(node_id + platform_id),不存 region / IP 类型等(那些进历史表)。

   **(b) 解锁历史表 (unlocks)** —— 仅供查看
   - 每次检测都**追加**一条,完整留痕,不覆盖。
   - 删除所属节点 / 平台时,该条历史会**连带删除**(见 2. 名词约定)。
   - 字段来自 MediaUnlockTest 的 `Result`:`status`(状态)、`region`(地区)、`info`(附加说明)、`err`(错误文本)+ node/platform/时间。
   - 查询接口 `GET /api/unlocks` 支持按 节点 / 平台 / `range` 筛选(**不分页**,按 `id desc` 返回数组)。
     `range` 形如 `24h` / `7d` / `30d`,后端按 `created_at >= now - 窗口` 过滤;
     非法值直接 400。当前 Web 页面用到 `node_id` + `range` 两个,展示形态见 5.1。
   - 不参与下发;按 `settings.retention_days` **定期清理**(见下第 5 点)。

5. **系统设置 (Setting)**
   - **`retention_days`(数据保留天数)**:unlocks 只保留最近 N 天,超期记录由服务端定时删除。
     默认 **7 天**;`0` = 不自动清理(永久保留);**不设上限**(填 99999 天 ≈ 永不清理);
     负数或非数字回退默认值。Web 在「系统配置 → 数据保留」里改,不需要重启。
     清理时机:Server 启动立即执行一次,之后每 **6 小时**一次;每次执行都重新读该配置,所以改完下次清理即生效。
     实现:独立包 `internal/retention` —— `retention.Days`(读配置,非法值回退默认)、
     `retention.Cleanup`(按 `created_at < now - N 天` 删除)、`retention.Start`(goroutine + ticker,
     间隔 `DefaultInterval` 由包自己决定)。`cmd/server` 只调一行 `retention.Start(db)`,不含业务细节。
   - **client 全局只读 Token**(供落地节点拉取 `unlocked`)。
   - 注:检测范围固定全部、Agent 定时(`scheduler`)在本地 `config.yml`,均不在此配置。

### 3.2 API 草案(REST)

```
# 认证
POST /api/auth/login              管理员登录 → JWT
POST /api/auth/change-password    修改密码(需 JWT;登录态下**不校验原密码**,body `{new_password}`,≥6 位)
GET  /api/userinfo                当前登录态 → `{user_id}`

# 公共(供登录页 / 仪表盘)
GET  /api/common/settings         公开(无需 JWT)→ `{title}`,登录页读取站点标题
GET  /api/common/stats            需 JWT → `{node_count, platform_count, association_count, active_node_count}`

# 节点
GET    /api/nodes                 节点列表(支持 `?search=` 按 name/alias/host 模糊;分页 limit/offset;每项 Preload `platforms[]` 关联平台对象,前端自取 name)
POST   /api/nodes                 新建节点(返回 节点 Token)
PUT    /api/nodes/:id             更新
DELETE /api/nodes/:id             删除(**事务连带清理**该节点的 node_platforms 关联与 unlocks 历史)
POST   /api/nodes/:id/retoken     重置 节点 Token

# 平台
GET    /api/platforms             平台列表(支持 `?search=` 按 name/rules 模糊;每项 Preload `nodes[]` 关联节点对象,前端自取 name/alias)
POST   /api/platforms             新建平台
PUT    /api/platforms/:id         更新
DELETE /api/platforms/:id         删除(**事务连带清理**该平台的 node_platforms 关联与 unlocks 历史)

# 解锁历史
GET    /api/unlocks               筛选查询(node_id / platform_ids / range;**不分页,直接返回数组**)
                                  range 形如 24h / 7d / 30d,按 created_at 只取该时间窗内的记录;
                                  不传则不限时间;格式非法直接 400(不静默忽略)
                                  platform_ids 逗号分隔多个平台(如 1,2,3),一次拉一批平台的记录;
                                   空片段跳过,任一段非法直接 400;与 node_id / range 之间是 AND
                                  node_id 必须是正整数;非法直接 400(不静默忽略)

# 系统设置
GET    /api/settings              → `{title, token, retention_days}`(未设置的键返回空串)
PUT    /api/settings              白名单键:
                                    - `title`(站点标题)
                                    - `token`(client 模式全局只读 Token)
                                    - `retention_days`(unlocks 保留天数,非负整数;0 = 不清理,不设上限)
                                    非法值(负数/非数字)直接 400
POST   /api/settings/retoken      重新生成全局只读 Token → `{token}`

# ===== Agent 专用(不用 JWT)=====
# node 模式:请求头 `Token: <节点 token>`
GET    /api/agent/node            拉取本节点连接信息(type/port/account/password),用于启动本机 socks/http 服务
POST   /api/agent/report          上报本节点解锁检测结果(按平台 name 上报;Server 匹配 platform 后:①覆盖更新 node_platforms 关联表 ②追加一条 unlocks 历史)。**name 在 platforms 表匹配不到的结果:静默丢弃 + 记一条日志,不自动建平台**(平台清单由管理员可控)
# client 模式:请求头 `Token: <全局只读 Token>`
GET    /api/agent/unlocked        拉取 `{node: {alias: connection}, platform: {name: {alias: [], rules: [], status}}}`。
                                  **全量下发**:禁用的平台 / 节点也一并返回并各自带 `status`(1=启用 / 0=禁用),
                                  由 client Agent 自行决定是否使用(不再由 Server 过滤)。
                                  注:`node` 里只包含"被某个平台引用的节点";没有任何平台关联的节点不会出现
```

`/api/agent/unlocked` 的 `data` 使用字典结构，便于 client 直接按节点 alias 和平台 name 查找：

```json
{
   "node": {
      "JPAK1": {
         "host": "jp.example.com",
         "port": 1080,
         "type": "socks5",
         "upload_at": "2026-09-12T16:30:15+08:00",
         "value1": "",
         "value2": "",
         "value3": "", "value4": "", "value5": "", "value6": "",
         "status": 1
      }
   },
   "platform": {
      "Netflix": {
         "alias": ["JPAK1"],
         "rules": ["domain:netflix.com", "domain:netflix.net"],
         "status": 1
      }
   }
}
```

### 3.2.1 统一响应格式

所有接口(含 Agent 专用)统一返回壳 `{ code, msg, data }`,由 `internal/utils` 的 `Response` + helper 输出:

```json
// 成功(HTTP 200/201)
{ "code": 200, "msg": "success", "data": { ... } }   // data 为资源本体
{ "code": 200, "msg": "success", "data": { "total": 42, "limit": 50, "offset": 0, "items": [...] } }  // 列表
{ "code": 200, "msg": "deleted" }                     // 无数据体(删除等)

// 失败(HTTP 4xx/5xx,code 与 HTTP 状态一致)
{ "code": 401, "msg": "invalid credentials" }
```

**约定**:HTTP 状态码为真实状态(不是一律 200),`code` 只是给前端可读的冗余;`data` 仅成功时出现(`omitempty`)。前端 axios 拦截器按 HTTP 状态判成败即可。列表统一走 `utils.Page`(壳内再套 `{total,limit,offset,items}`)。

### 3.3 数据模型(核心表)

> 全项目字段定义的**唯一来源**。3.1 各功能模块只描述行为,字段一律看这里。

#### users(管理员)

| 字段 | 键名 | 类型 | 说明 |
|------|------|------|------|
| 用户ID | `id` | int | 主键 |
| 用户名 | `username` | string | 登录名,唯一 |
| 密码哈希 | `password_hash` | string | 加密存储 |
| 创建时间 | `created_at` | datetime | |

> 系统仅单一管理员角色,无 `role` 字段。

#### nodes(解锁节点)—— 仅存 node 节点

| 字段 | 键名 | 类型 | 说明 |
|------|------|------|------|
| 节点ID | `id` | int | 主键 |
| 节点名称 | `name` | string | 展示用名称 |
| 节点标识 | `alias` | string | **唯一**,业务引用键 |
| 节点类型 | `type` | string | `socks5` / `http`(小写,当前仅两种);枚举可扩展 |
| 节点状态 | `status` | int | `1`=启用、`0`=禁用(当前仅两态,后期可扩) |
| 节点地址 | `host` | string | 域名或 IP,**管理员在 Web 手填**(落地节点据此连接);Agent 不自动回填 |
| 节点端口 | `port` | int | 端口 |
| 节点账号 | `value1` | string | 代理账号(可空) |
| 节点密码 | `value2` | string | 代理密码(可空) |
| 保留字段 | `value3`~`value6` | string | 预留,供后期新协议使用 |
| 节点令牌 | `token` | string | Agent 认证用,每节点独立 |
| 最近上报 | `report_at` | datetime | 反映活跃度 |
| 创建时间 | `created_at` | datetime | |
| 更新时间 | `updated_at` | datetime | |

#### platforms(平台)

| 字段 | 键名 | 类型 | 说明 |
|------|------|------|------|
| 平台ID | `id` | int | 主键 |
| 平台名称 | `name` | string | 如 Netflix、Disney+、ChatGPT。**与 MediaUnlockTest 测出的平台名一一对应**,作为检测结果匹配键 |
| 分流规则 | `rules` | json/string | 域名/路由列表(如 `domain:sky.de`),client 生成配置时用;**非检测规则** |
| 平台状态 | `status` | int | `1`=启用、`0`=禁用(当前仅两态,后期可扩) |
| 创建时间 | `created_at` | datetime | |
| 更新时间 | `updated_at` | datetime | |

#### node_platforms(节点-平台关联)—— 多对多 / 下发数据源

由 GORM 依据 `Node.Platforms` / `Platform.Nodes` 的 `many2many:node_platforms` 标签**自动生成**,无独立 struct。

| 字段 | 键名 | 类型 | 说明 |
|------|------|------|------|
| 节点ID | `node_id` | int | 复合主键 / 外键 → nodes |
| 平台ID | `platform_id` | int | 复合主键 / 外键 → platforms |

> 复合主键 `(node_id, platform_id)` 天然去重。每条即"该节点当前解锁该平台",只记录关系本身;**仅 `status==1`(成功解锁)才在此表**。

#### unlocks(解锁历史)—— 仅供查看,不参与下发

| 字段 | 键名 | 类型 | 说明 |
|------|------|------|------|
| ID | `id` | int | 主键 |
| 节点ID | `node_id` | int | 外键 → nodes |
| 平台ID | `platform_id` | int | 外键 → platforms |
| 状态 | `status` | int | MediaUnlockTest 原始数值:`1`=成功、`2`=受限、`3`=不支持、`4`=封禁、`5`=失败、`6`=异常、`-1`=网络错误、`-2`=错误 |
| 地区 | `region` | string | 解锁地区(US/JP…),可空 |
| 附加说明 | `info` | string | 如 "VPN Blocked"、"429 Rate limit",多为空 |
| 错误文本 | `err` | string | 失败时原始错误文本(`connection refused`…),成功为空;存 string 不存 error |
| 创建时间 | `created_at` | datetime | |
| 更新时间 | `updated_at` | datetime | |

> 不含 latency / ip_type / method —— MediaUnlockTest 不产出。

#### settings(系统设置)

| 字段 | 键名 | 类型 | 说明 |
|------|------|------|------|
| 键 | `key` | string | 配置项名 |
| 值 | `value` | string/json | 配置值 |

常用配置项:

| 键名 | 默认值 | 说明 |
|------|--------|------|
| `title` | `MediaUnlock` | 站点标题(登录页 / 侧边栏) |
| `token` | 随机 32 位 hex | **client 全局只读 Token**(落地节点拉 `unlocked` 用) |
| `retention_days` | `7` | unlocks 保留天数。`0` = 不自动清理;不设上限。Server 启动清一次,之后每 6 小时一次 |

注:`retention_days` 仅影响 `unlocks` 表(历史记录);`nodes` / `platforms` / `node_platforms` 不会被清理。

---

## 4. Agent(节点代理)

单个 Go 二进制,启动时读取本地 **`config.yml`**(YAML 格式):`api`、`mode`、认证凭证、`scheduler` 等。
定时逻辑:**按 `scheduler`(cron 表达式,6 段含秒)定时执行**。

### 4.1 通用行为

- 所有运行参数(`scheduler` 定时、认证凭证等)来自**本地 `config.yml`**,Server 不下发 Agent 配置。
- **两种模式都做全平台解锁检测**(检测范围固定全部,无需配置);区别在结果用途:**server 上报**给 Server,**client 仅本地对比**(不上报)。
- 无心跳:当前不跟踪 Agent 在线状态。server 节点的活跃度由"最近上报时间"体现。

### 4.2 Server 模式(解锁节点)—— 起服务 + 测自己 + 上报

1. **拉取自身节点信息**:带**节点 token** `GET /api/agent/node`,取回本节点的 `type`(`socks5`/`http`)、`port`、`value1`(账号)、`value2`(密码)等。
2. **开启本机代理服务**:按上一步的信息在本机启动对应的 **SOCKS5 / HTTP** 服务(监听 `port`,按账号密码鉴权),供落地节点连接、转发需解锁的流量。
3. 对本机执行**全部平台的解锁检测**——调用**内置(fork 自 MediaUnlockTest `pkg/`)的检测代码**(`core` + `providers`),无需外部二进制。接入方式见 [4.5](#45-mediaunlocktest-接入方式)。
4. 组装结果,带**节点 token** `POST /api/agent/report` 上报到 Server。

> 节点连接信息在 Web 后台维护、存节点表;server Agent 拉下来后据此起服务,落地节点则从 `unlocked` 拿到同一份连接信息去连它。

### 4.3 Client 模式(落地节点)—— 本地检测 + 对比 + 生成配置,不上报

1. **本地解锁检测**:在本机(落地节点)跑**全平台解锁检测**(与 server 同一套内置检测代码),得出本机 **已解锁 / 未解锁** 的平台集合。
2. **拉取 API 数据**:`GET /api/agent/unlocked` 取每个已解锁平台的 **`rules`(分流域名/路由)** + **对应解锁节点连接信息**(host/port/type/账号密码)。
3. **对比取交集**:对每个**本机未解锁**的平台,查 API 是否有解锁节点能解它——
   - 有 → 该平台需要分流,进入下一步;
   - 无 → 跳过(本机解不开、也没人能解);
   - 本机**已解锁**的平台 → 不分流,直接走本地出口(省一跳)。
4. **按 `stack` 解析地址 + 测速选节点**:对候选解锁节点按 `stack` 解析地址(见下方 stack 说明,解不出/协议栈不符则移除),做 TCP 延迟探测筛掉不通的;**同一平台有多个可用解锁节点时,选延迟最短的**。
5. **生成配置**:把这些平台的 `rules` 域名 → 指向选定解锁节点(用解析后的地址作 `host`),**组装并生成配置文件**,供 soga 按域名做**分流**。使用 Agent **内置的固定模板**生成(每个解锁节点一个 `[[routes]]` 块,块内按平台罗列 `rules`,配 `[[routes.Outs]]` 的 socks/http 出口;末尾一条 `rules=["*"]` 的 `direct` 兜底),模板不经 Server 下发。**XrayR 项目已废弃,当前 `render.type` 仅支持 `soga`。**

**`stack`(仅 client 生效)** —— 是**生成配置的协议栈**:先按它解析出节点地址(该地址既用于**测速**、也写入**生成的配置**)。

| 值 | 行为 |
|----|------|
| `default` | 默认。直接用节点原始 `host`;若是域名则保留域名,由系统/运行环境自行决定走 IPv4 还是 IPv6,**不提前强制解析**。 |
| `ipv4` | 强制 IPv4。域名先做 DNS 取 A 记录的 IPv4,用该 IPv4 做延迟探测并作为写入配置的 `host`;解析不到 IPv4、或原始 host 本身是 IPv6 → 该节点**被移除**。 |
| `ipv6` | 强制 IPv6。域名先做 DNS 取 AAAA 记录的 IPv6,用该 IPv6 做延迟探测并作为写入配置的 `host`;解析不到 IPv6、或原始 host 本身是 IPv4 → 该节点**被移除**。 |

> - `stack` **仅作用于 client 生成配置**,**不是解锁检测的协议栈开关**:client 的本地解锁检测**固定走 IPv4**、与 `stack` 无关;`stack` 只管"生成配置时节点地址写成什么"。也不影响 node 模式。
> - 流程是 **`stack` 解析地址 → 用该地址测速/筛选 → 写入配置**;无论哪种 `stack`,延迟探测都会执行。
> - **DNS 解析不使用本机 resolver**,固定用公共 DNS **`8.8.8.8`、`1.1.1.1`**(ipv4/ipv6 解析域名时)。

> 注:client 的**本地检测只为对比、决定分流**,不上报、不写库、不心跳。Server 端也不需要跟踪 client 是否在线。

### 4.4 Agent 配置示例

Agent 配置文件统一用 **YAML(`config.yml`)**。所有本地时间参数(如 `scheduler` 定时)都在本地配置里,不从 Server 下发。

```yaml
# node 模式(解锁节点)—— config.yml
api: https://panel.example.com
mode: node
token: xxxxxxxx                  # 节点专属 Token(节点表里那个)
scheduler: "0 45 * * * *"       # cron(6 段含秒)定时检测+上报;此例=每小时 45 分 00 秒
# 检测范围固定为全部平台,无需配置
```

```yaml
# client 模式(落地节点)—— config.yml
api: https://panel.example.com
mode: client
token: xxxxxxxx                  # 全局只读 Token
scheduler: "0 45 * * * *"       # cron(6 段含秒)定时本地检测+拉取+生成;此例=每小时 45 分 00 秒
stack: default                  # 生成配置的协议栈:default | ipv4 | ipv6(仅 client 生效)
render:                     # 渲染生成配置文件的目标
  type: soga                # 仅 soga(XrayR 已废弃)
  path: /etc/soga/routes.toml
```

### 4.5 MediaUnlockTest 接入方式

因为**需要修改它的部分检测逻辑**(它有些平台的解锁检测已失效,要自行修复/调整),所以**不作为外部依赖引用,而是把它的检测代码 fork 进本项目、当自己的代码维护**。

**只需要它的 `pkg/`**(`core` + `providers` 就是全部检测逻辑),`cli/`、docs、Dockerfile 等都不要。

fork 步骤:
1. 从 [MediaUnlockTest/pkg](https://github.com/HsukqiLee/MediaUnlockTest/tree/main/pkg) 下载 `pkg/core` + `pkg/providers` 到本项目的 `pkg/{core,providers}`(目录名沿用 `pkg` 即可)。
2. **改 import 路径**:源码里的路径是 `MediaUnlockTest/pkg/...`,你要的是 `你的module/pkg/...`,后半截 `pkg/...` 完全一致,所以**只需把前缀 `MediaUnlockTest` 全局替换成你的 module 名**。一条 sed 即可:
   ```bash
   grep -rl 'MediaUnlockTest/' pkg/ | xargs sed -i '' 's#MediaUnlockTest/#你的module名/#g'
   ```
   替换后不再需要 `replace` 指令,"MediaUnlockTest" 名字从项目里彻底消失。
3. **合并依赖**:把它用到的外部依赖(`tls-client` 等)并进本项目 `go.mod`,`go mod tidy`。**建议照抄它 `go.sum` 里的版本**,避免拉到不兼容的新版编译不过。
4. 之后 provider 里失效的检测**直接改本地代码**即可。保留 MediaUnlockTest 的 LICENSE / 版权归属以合规。

**检测项来源**:`pkg/providers` 以**导出变量**暴露各地区检测项切片(约 12 个):`GlobeTests`、`TaiwanTests`、`HongKongTests`、`JapanTests`、`KoreaTests`、`NorthAmericaTests`、`SouthAmericaTests`、`EuropeTests`、`AfricaTests`、`SouthEastAsiaTests`、`OceaniaTests`、`AITests`(另有 `NetflixRegion`)。每个元素是 `TestItem{ Name string; Func func(client) core.Result; SupportsV6 bool }`;`core.Result` = `{ Status int; Region string; Info string; Err error }`。

- **检测**(server 与 client 都用):把这些切片合并遍历,对每个 `item.Func(client)` 调用,拿到 `core.Result`。server 组装后上报;client 用它得出本机未解锁平台做对比。地区组装逻辑原在它 `cli/main`(未导出),**我们自己按上面变量重新组装**,不依赖 main 包。
- **平台名匹配**:`item.Name` 与 `platforms.name` 一一对应(匹配键)。若后期要做一键同步,遍历这些切片收集 `item.Name` 即可得到全部平台清单——当前不做。

**注意事项**:
- MediaUnlockTest 要求 **Go 1.26.4**,本项目 go.mod 需对齐。
- 依赖较重(`tls-client` 模拟 TLS 指纹等 40+ 间接依赖),会增大 Agent 二进制体积——可接受,但需知晓。
- fork 后与上游脱钩:上游新增/修复的平台**不会自动同步**,需要时手动比对合并(这是可修改检测逻辑的代价,已接受)。

---

## 5. Web(管理后台)

页面对应 Server 的模块:

1. **登录页** — 账号密码(读取公开站点标题、主题切换)。
2. **仪表盘** — 统计卡片(节点总数 / 平台总数 / 关联总数),数据取 `GET /api/common/stats`。
3. **节点列表** — 表格 + Agent 自动检测得到的关联平台 + 最近上报时间;新增/编辑/删除。
4. **平台列表** — 平台管理(CRUD)+ 关联节点详情。
5. **解锁历史** — 选一个节点,按平台查看历次检测结果(**一行一个平台**),见 5.1。
6. **系统设置** — 站点标题 `title`、**数据保留天数 `retention_days`**(默认 7 天,0 = 不清理,不设上限)、client 全局只读 Token(查看 / 重新生成,走确认弹窗)、修改密码(**行内表单**:同一行「新密码 + 确认新密码」两个输入框 + 提交按钮,不用弹窗;成功后自动退出登录)。

技术:**Vite + React + TypeScript + Tailwind v4 + Radix UI**;路由用 `react-router-dom` v7 的 `HashRouter`(布局与风格参考 [satnaing/shadcn-admin](https://github.com/satnaing/shadcn-admin))。实现取舍:
- **UI 原语**:Radix UI(`select` / `dialog` / `dropdown-menu` / `switch` / `label` / `toast` / `tooltip` / `slot` / `separator`)+ Tailwind,按 shadcn 风格拆成 `src/components/ui/*.tsx` 多文件;**不跑 shadcn CLI**,原语代码直接入库。
- **认证**:本项目 JWT(存 localStorage 的 `token`),axios 请求拦截器注入 `Bearer`;响应拦截器在 401 时清 token 并跳 `#/login`(登录接口本身除外)。
- **数据获取**:各页面直接用 `useEffect` + `useCallback` 自行管理 loading / error;**未引 react-query,也没有 `useAsync` hook**。
- **路由**(HashRouter,刷新不会 404):`/login` + 受保护布局(`ProtectedRoute`);子页 `/dashboard` `/nodes` `/platforms` `/unlocks` `/settings`;单一管理员,登录即全权。
- 开发期 `vite` 把 `/api` 代理到本地 Server(`:8080`);**生产由 Server 直接托管前端构建产物**——`web/dist` 经 `//go:embed` 打进 Server 二进制(`server/internal/static`),真正单文件部署。构建见 `scripts/build.sh`(前端 build → 同步进嵌入目录 → `go build`)。Server 路由:命中的 `/api/*` 走业务、未命中 `/api/*` 返回 JSON 404;其余 GET 命中 `dist` 内文件直接返回,否则回退 `index.html` 交前端路由(刷新 `/nodes` 等不 404)。

### 5.1 解锁历史页(`/unlocks`)

顶部有**节点下拉**(默认选中第一个节点)和**时间范围**切换(`24小时` / `7天` / `全部`,默认 **`24小时`**),
下面是**用 `Table` 组件**展示的检测历史(`unlocks` 页和 `nodes` / `platforms` 页视觉一致);`全部` 不传 `range` 参数,等价于后端不限时间。

表头结构(两列):

| TableHead | 内容 |
|---|---|
| 平台(`w-56`) | 平台名,只显示文字 |
| 检测历史 | **8 个图例色块**(成功 / 受限 / 不支持 / 封禁 / 失败 / 异常 / 网络错误 / 错误),`justify-end` 居右排列在表头里 |

注意:页面上**没有 `CardHeader`**(连"检测历史"卡片标题都没有了)—— 表格直接开始,
和 `nodes` / `platforms` 页保持一致;第二列表头就是图例位置,不额外占行。

**图例只出现在桌面端**。移动端(卡片布局)**不展示图例** —— 点格子后的悬浮详情里带状态文字,
已经足够表达颜色含义,省掉图例那一行高度(实测约 61px)。

要点:

- **加载方式:平台分页 + 批量拉记录 + 无限滚动**
  1. `GET /api/platforms?limit=20&offset=<n>` 取一页平台(前端按 `page` 换算成 `offset`,第 2 页为 20)
  2. `GET /api/unlocks?node_id=<id>&platform_ids=<这批平台 id 逗号分隔>&range=<档位>`
     一次把这批平台的检测记录全拉回来(不传 `limit`,要窗口内全部)
  3. 页面滚到底部(`IntersectionObserver` 哨兵,提前 120px 触发)自动加载下一页平台
  4. 切换节点或时间范围 → 清空,回到第 1 页
- **行的来源 = 全部平台**(按平台 id 排序),**无论该节点是否解锁它**。
  未解锁或从未检测过的平台同样出行,只是历史列为空。
  这样"某平台最近一次失败了"不会让它的历史凭空消失——`node_platforms` 的语义是
  "最近一次检测**成功**的平台"(见 3.1 第 4 条),失败即摘掉关联,若按它过滤行就会丢历史。
- 格子颜色按状态区分(见 3.3 `unlocks.status`)。
- **悬浮格子显示详情**:平台 / 状态 / 地区 / 时间 / 说明(`err` 或 `info`,两者互斥)。
- 后端 `GET /api/unlocks` 按 `id desc` 返回,前端整体反转成正序后再按平台分组,保证每组内部也是旧→新。
- **时间范围由后端过滤**:前端把选中的档位作为 `range` 参数传给 `GET /api/unlocks`
  (当前三档 `24h` / `7d` / 不传;后端支持任意 `<n>h` / `<n>d`),后端按 `created_at >= 下界` 过滤。
  切换档位会重新请求。默认档位是 `24h`。
- **不分页**:`GET /api/unlocks` 直接返回该时间窗内的**全部记录(数组)**,没有 `limit` / `offset`。
  单次数据量由"平台分页 + `platform_ids` 批量拉 + 滚到底无限加载"控制(见上)。
- 该范围内没有任何记录的平台,历史列显示"该范围内无记录"。

---

## 6. 关键数据流(端到端)

```
[node-mode Agent] GET /api/agent/node ─▶ 取本节点 type/port/账号密码 → 起本机 SOCKS5/HTTP 服务
[node-mode Agent] 检测本节点解锁
   └─ POST /api/agent/report ─▶ [Server] ①status==1 的平台 → 覆盖更新 node_platforms(下发用)
                                          ②所有结果 → 追加 unlocks(历史,仅查看)
                                    │
[Client-mode Agent] 定时轮询(不上报)
   ①本地检测 ─▶ 本机未解锁平台集合
   ②GET /api/agent/unlocked ─▶ 各平台可由哪些解锁节点解锁 + rules + 节点连接信息
   ③取交集(本机未解锁 且 API 有节点能解)→ stack 解析 + 测速,同平台多节点选最短
   ④生成分流配置 ─▶ soga 按域名把这些平台转发到选定解锁节点
   (本机已解锁的平台不分流,直接本地出口)
```

> 本系统的直接消费方是 **soga 这类服务端软件**,不是终端用户。client Agent 通常与 soga 同机部署,产出的配置文件由它加载;终端用户是连 soga 的,不直接接触本系统。

---

## 7. 工程结构(目录)

> 单仓库(monorepo)三端并列:`server/`(后端 Server)、`agent/`(Agent,内置 fork 的 `pkg/`)、`web/`(前端)。

```
MediaUnlock/
├── 需求文档.md
├── README.md
├── docker-compose.yml               # 空占位文件,未使用(部署方式见 §5:单二进制直接运行,不依赖容器)
├── .gitignore
├── scripts/
│   └── build.sh                     # 前端 build → 同步进嵌入目录 → go build
│
├── server/                          # 后端 Server(Gin + GORM + SQLite;打包单二进制)
│   ├── go.mod / go.sum
│   ├── cmd/server/main.go           # 入口:读 config.yml → 初始化 DB(含种子)→ 挂后台清理 → 装配路由 → 启动
│   ├── config.example.yml           # Server 配置示例(端口/DB 路径/JWT 密钥等)
│   ├── internal/
│   │   ├── config/                  # 解析 config.yml(端口、DB 路径、JWT 密钥)
│   │   ├── database/                # GORM 初始化 + AutoMigrate + 启动种子
│   │   │   └── database_test.go     #   种子测试(固定 admin / 默认 settings / 幂等)
│   │   ├── models/                  # 5 张表 struct:user/node/platform/
│   │   │                            #   unlock/setting
│   │   │                            #   (node_platforms 是 many2many 关联表,无 struct)
│   │   ├── handlers/                # HTTP 处理器,按资源分文件:
│   │   │   ├── auth.go              #   /api/auth/login、change-password
│   │   │   ├── node.go              #   /api/nodes CRUD + retoken
│   │   │   ├── platform.go          #   /api/platforms CRUD
│   │   │   ├── unlock.go            #   /api/unlocks
│   │   │   ├── setting.go           #   /api/settings
│   │   │   ├── common.go            #   /api/common/settings|stats
│   │   │   └── agent.go             #   /api/agent/node|report|unlocked
│   │   ├── middleware/              # JWT / 节点 token / 全局只读 Token 校验
│   │   ├── retention/               # 数据保留策略(Days / Cleanup / Start)
│   │   │   └── retention_test.go    #   保留天数读取 + 超期清理
│   │   ├── router/                  # 路由装配(handler 挂到 Gin;main 与测试共用)
│   │   │   ├── router_test.go       #   路由级测试公共部分(setup / do / doAgent / 登录)
│   │   │   ├── node_test.go         #   /api/nodes 的 CRUD / 分页 / 搜索 / type 白名单
│   │   │   ├── platform_test.go     #   /api/platforms
│   │   │   ├── unlock_test.go       #   /api/unlocks
│   │   │   ├── setting_test.go      #   /api/settings
│   │   │   ├── common_test.go       #   /api/common/*、改密、settings/retoken
│   │   │   ├── agent_test.go        #   /api/agent/*(node 模式 / client 模式)
│   │   │   └── web_test.go          #   静态资源 + SPA 回退 + /api 404
│   │   ├── utils/                   # 统一响应壳 {code,msg,data}、分页、随机串
│   │   └── static/                  # //go:embed 的前端构建产物(web/dist 同步而来)
│   └── data/                        # SQLite 文件落地(database.db,gitignore)
│       # 注:service/ 层尚未创建 —— 逻辑简单时先写在 handlers 里,变复杂再抽
│
├── agent/                           # Agent(单二进制,内置 fork 的 pkg/)
│   ├── go.mod / go.sum              # module 名建议就叫 agent
│   ├── cmd/agent/main.go            # 入口:读 config.yml → 按 mode 分发 → 起 cron
│   ├── internal/
│   │   ├── config/                  # 解析 config.yml(api/mode/token/scheduler/stack/render)
│   │   ├── detect/                  # 调 pkg/ 跑全平台检测,归一成结果
│   │   ├── node/                    # node 模式:拉 node → 起 socks/http → 检测 → 上报
│   │   │   ├── run.go
│   │   │   └── proxy.go             #   本机 SOCKS5/HTTP 服务
│   │   ├── client/                  # client 模式:检测 → 拉 unlocked → 对比 → 选路 → 生成配置
│   │   │   ├── run.go
│   │   │   ├── stack.go             #   选路准备:stack 地址解析(固定 DNS 8.8.8.8/1.1.1.1)
│   │   │   │                        #     + TCP 延迟探测(多节点选最优)
│   │   │   └── render.go            #   渲染生成 soga 配置(target;将来支持其他格式再拆子包)
│   │   ├── api/                     # 封装对 Server 的 REST 调用 + 鉴权头
│   │   └── scheduler/               # cron(6 段含秒)调度
│   ├── pkg/                         # ← fork 自 MediaUnlockTest,import 前缀改为 agent/pkg
│   │   ├── core/                    #   Result 等基础类型
│   │   ├── providers/               #   各地区 TestItem 列表(可在此修复失效的解锁)
│   │   └── LICENSE                  #   保留原 LICENSE
│   └── config.example.yml           # 两种模式示例(node / client)
│
├── web/                             # 前端(Vite + React + TS + Tailwind + Radix,参考 satnaing/shadcn-admin)
│   ├── package.json / vite.config.ts / eslint.config.js / index.html
│   ├── tsconfig.json / tsconfig.app.json / tsconfig.node.json
│   ├── public/
│   ├── dist/                        # 构建产物(同步进 server/internal/static,gitignore)
│   └── src/
│       ├── main.tsx                 # 挂载入口
│       ├── App.tsx                  # HashRouter + 路由表 + 登录守卫
│       ├── index.css                # Tailwind 入口 + 主题变量
│       ├── pages/                   # 一页一文件:Login / Dashboard / Nodes /
│       │                            #   Platforms / Unlocks / Settings
│       ├── components/              # Layout / ProtectedRoute / ThemeProvider
│       │   ├── theme-context.ts     #   Theme 类型 + Context + useTheme
│       │   └── ui/                  # shadcn 风格原语(Radix + Tailwind),一组件一文件
│       └── lib/                     # api.ts(axios 封装 + 各资源接口)、utils.ts(cn)
```

> `deploy/`(Dockerfile、systemd 模板)尚未创建;`docker-compose.yml` 是空占位文件、未被使用。
> 部署走 §5 的**单二进制**路线(`server/server` 已内嵌前端,直接运行即可),因此当前**不依赖容器**。

说明:
- **`agent/pkg/`** 是 fork 自 MediaUnlockTest 的检测代码(详见 [4.5](#45-mediaunlocktest-接入方式)),import 前缀已改为本项目 module;可在此直接修复其失效的解锁项。
- **`server/internal/service/`** 为**可选层,尚未创建**:表不多、逻辑简单时先把业务写在 `handlers/` 里,某个 handler 变复杂再抽出。
- **测试**:Server 共 44 个测试函数(9 个测试文件),覆盖 config / database / retention / router 四块;
  其中路由级测试按资源拆成 `router/` 下的多个 `*_test.go`。约定:测试文件**跟随源文件名**
  (`foo.go` ↔ `foo_test.go`);路由级测试用真实 Gin 引擎 + `t.TempDir()` 临时库,**不 mock、不碰开发库**;
  数据尽量走真实接口灌入(如 `POST /api/agent/report`),而非直接塞表。
- **`server/data/database.db`**(SQLite)、`node_modules/`、`web/dist/`、`server/internal/static/` 里的同步产物等应写入 `.gitignore`。

---

## 8. 里程碑建议

1. **M1 Server 骨架**:登录 + 节点/平台 CRUD + Agent 节点信息/上报/拉取接口 + 数据库 + token 认证。
2. **M2 Agent node 模式**:拉取节点信息起 socks/http 服务 + 接入 MediaUnlockTest 检测 + 带节点 token 上报。
3. **M3 Agent client 模式**:本地检测 + 拉取 API 已解锁数据对比 + 对本机未解锁平台生成分流配置(stack 解析/测速选节点)。
4. **M4 Web 后台**:登录 / 仪表盘 / 节点 / 平台 / 解锁历史 / 系统设置 六个页面。
5. **M5 打磨**:系统设置、数据保留清理。

---

## 9. 待确认 / 决策点

- ~~数据库选型~~:已定 **SQLite**(GORM 驱动,代码保持可切换,后续量大再换 PG)。
- ~~MediaUnlockTest 接入方式~~:已定 **fork 它的 `pkg/`(core+providers)进本项目、改 import 当自己代码**(因为要修复它部分失效的检测逻辑)。检测基于 `pkg/providers` 的导出检测项切片。详见 [4.5](#45-mediaunlocktest-接入方式)。
- ~~禁用语义~~:已定 **禁用不影响检测/上报/入库/起服务**。平台禁用仍写 node_platforms 与 unlocks;节点禁用其 Agent 仍起服务。`GET /api/agent/unlocked` 为**全量下发**(禁用项也返回,带 `status`),由 client Agent 自行跳过。
- ~~前端框架~~:已定 **React + Vite**。
- ~~上报平台名匹配不到~~:已定 **静默丢弃 + 记日志,不自动建平台**。
- ~~status 枚举~~:已定 **`1`=启用、`0`=禁用**(节点/平台同,当前仅两态)。
- ~~初始管理员~~:已定 **首启无 admin 时自动创建,随机密码打印到启动日志**,不硬编码弱口令。
- ~~节点 host 来源~~:已定 **管理员在 Web 手填**,Agent 不自动回填。
- ~~解锁历史的展示形态~~:已定 **桌面用 `Table`(两列:平台 / 检测历史)、移动端用卡片列表(上=平台名 / 下=解锁格子)**,不用 `CardHeader`(无"检测历史"卡片标题),与 `nodes` / `platforms` 页视觉一致。
- ~~图例展示范围~~:已定 **仅桌面端**(表头第二列 `justify-end` 居右);**移动端不展示** —— 点格子后的悬浮详情里带状态文字,足够表达颜色含义。
- ~~`GET /api/unlocks` 是否分页~~:已定 **不分页**,直接返回符合条件的全部记录(数组)。前端靠**平台分页 + `platform_ids` 批量拉 + 滚到底无限加载**控制单次数据量;不再有 `limit` / `offset`,非法 `range` / `platform_ids` 直接 400。
- ~~时间范围档位~~:已定 **`24小时` / `7天` / `全部` 三档,默认 `24小时`**(`全部` = 不传 `range`,后端不限时间)。
- ~~数据保留天数上限~~:已定 **不设上限**;`retention_days` 只需是非负整数,默认 7 天,`0` = 不自动清理。定期清理:启动一次 + 每 6 小时一次。
